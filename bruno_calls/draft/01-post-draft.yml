info:
  name: POST /draft
  type: http
  seq: 1

http:
  method: POST
  url: "{{api}}/draft"
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
