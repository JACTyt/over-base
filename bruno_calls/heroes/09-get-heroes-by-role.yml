info:
  name: GET /heroes?role={role}
  type: http
  seq: 9

http:
  method: GET
  url: "{{api}}/heroes?role=tank"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
