info:
  name: GET /heroes/{hero_id}
  type: http
  seq: 8

http:
  method: GET
  url: "{{api}}/heroes/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
