info:
  name: Skills ban
  type: http
  seq: 4

http:
  method: POST
  url: "{{api}}/skills_ban"
  body:
    type: json
    data: |-
      {
          "count": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
