info:
  name: Skills combine
  type: http
  seq: 3

http:
  method: POST
  url: "{{api}}/skills_combine"
  body:
    type: json
    data: |-
      {
          "count": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
